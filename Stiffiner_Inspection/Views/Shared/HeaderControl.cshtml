<div id="header-control" class="d-flex justify-content-between">
    <div id="time" class="d-flex align-items-center">
        <div id="date">
            Thursday, Jan 18, 2024
        </div>
        <div id="current-time">
            13:33:53
        </div>
    </div>

    <div id="action" class="d-flex align-items-center">
        <div class="option-model d-flex align-items-center">
            <label for="">Model</label>
            <select name="" id="">
                <option value="">All</option>
                <option value="1">1</option>
                <option value="2">2</option>
            </select>
        </div>

        <div class="option-time">
            <label for="">Search</label>
            <i class="fa fa-clock-o" aria-hidden="true"></i>
            <select name="" id="">
                <option value="">Start time to end time</option>
                <option value="">2</option>
            </select>
        </div>

        <a class="btn btn-setting" onclick="Meee2()">
            <i class="fa fa-cog" aria-hidden="true"></i>
            Setting
        </a>
        <a href="#" class="btn btn-primary btn-export-excel" onclick="Meee()">
            Export report
            <i class="fa fa-arrow-right" aria-hidden="true"></i>
        </a>
    </div>
</div>

<script>
    function Meee() {
        $.ajax({
            url: '@Url.Action("GetData", "Home")',
            type: 'GET',
            success: function (data) {
                // Display received data
                //$('#result').html('Name: ' + data.Name + ', Age: ' + data.Age);
                //  alert(data);
            },
            error: function () {
                // Handle errors
                $('#result').html('Error occurred.');
            }
        });
    }
    function Meee2() {
        $.ajax({
            url: '@Url.Action("Setdata", "Home")',
            type: 'GET',
            success: function (data) {
                // Display received data
                //$('#result').html('Name: ' + data.Name + ', Age: ' + data.Age);
                //  alert(data);
            },
            error: function () {
                // Handle errors
                $('#result').html('Error occurred.');
            }
        });
    }
</script>